# Copy to .env.local for local development
# For production: set VITE_SOCKET_URL in your host's build settings (Cloudflare Pages, etc.)

# Backend API + Socket.IO URL (no trailing slash)
VITE_SOCKET_URL=http://localhost:3001
